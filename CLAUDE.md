@AGENTS.md

## Site editor

`src/content/edits.json` is written by the Foundation's site editor (`/editor`), which commits to `main`. Pull before pushing, never hand-edit it casually, and when a content change moves, renames or removes a field, check whether an edit in that file points at it (see README → Site editor).
